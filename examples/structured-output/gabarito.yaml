name: structured-output
version: v1
description: Example gabarito for validating provider-level structured output.
cases:
  - id: summary-001
    input: "Cliente relatou atraso no pagamento, mas prometeu quitar em 7 dias."
    expected_output: |
      {
        "type": "object",
        "additionalProperties": false,
        "required": ["summary", "risk"],
        "properties": {
          "summary": {"type": "string"},
          "risk": {"type": "string", "enum": ["low", "medium", "high"]}
        }
      }
    assertion:
      type: json_schema
    weight: 1.0
    tags: [structured-output, json-schema]
